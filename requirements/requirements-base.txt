# Base Requirements - Works with Python 3.8+
# Core dependencies that work across all supported Python versions

# MCP server framework
mcp>=1.13.1

# Neural embeddings runtime
onnxruntime>=1.20.1
tiktoken>=0.9.0

# AsyncIO support
asyncio-mqtt>=0.16.0  # Optional, for MQTT support

# Code-specific embeddings (Phase 1 L9 upgrade)
sentence-transformers>=2.2.0  # For Qodo-Embed and other code models