# L9 Neural Flow - Core Dependencies
# Production-tested versions for enterprise deployment

# Vector Database
qdrant-client>=1.7.0
chromadb>=0.4.0

# Neural Embeddings
sentence-transformers>=2.2.0
tiktoken>=0.5.0
onnxruntime>=1.16.0

# MCP Protocol (if available via pip)
# mcp>=0.1.0  # Currently needs to be installed separately

# HTTP Client for model server
aiohttp>=3.8.0
httpx>=0.24.0

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0