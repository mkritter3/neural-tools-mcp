# L9 Neural Tools - Environment Configuration Template
# Copy this file to .env and customize the values for your environment

# Project Configuration
PROJECT_NAME=my-neural-project
PROJECT_DIR=../

# Security Configuration (REQUIRED)
# Generate a secure password for your Neo4j database
NEO4J_PASSWORD=your-secure-neo4j-password-here

# Debug Port Configuration (Optional)
# Uncomment and modify these if you need debug access to services
# QDRANT_DEBUG_PORT=6681         # Qdrant REST API debug port
# NOMIC_DEBUG_PORT=8081          # Nomic embeddings debug port  
# NEO4J_DEBUG_HTTP_PORT=7475     # Neo4j HTTP UI debug port
# NEO4J_DEBUG_BOLT_PORT=7688     # Neo4j Bolt protocol debug port

# Performance Configuration (Optional)
# L9_PERFORMANCE_MODE=enhanced    # Performance optimization mode
# OMP_NUM_THREADS=6              # OpenMP thread count
# TOKENIZERS_PARALLELISM=false   # Disable tokenizer parallelism for stability

# Neural Embeddings Configuration (Optional)
# EMBEDDING_MODEL=nomic-ai/nomic-embed-text-v2-moe
# MODEL_TRUST_REMOTE_CODE=true
# MAX_CONCURRENT_REQUESTS=1024
# MAX_BATCH_TOKENS=32768
# INFERENCE_OPTIMIZATION=true
# MOE_ROUTING_EFFICIENCY=0.85

# Neural Embeddings Performance (Optional)
# TORCH_COMPILE=false
# FLASH_ATTENTION=true
# BATCH_PROCESSING=dynamic

# Qdrant Configuration (Advanced)
# QDRANT__SERVICE__HTTP_PORT=6333
# QDRANT__SERVICE__GRPC_PORT=6334
# QDRANT__LOG_LEVEL=INFO
# QDRANT__STORAGE__PERFORMANCE__INDEXING_THRESHOLD_KB=10000
# QDRANT__STORAGE__PERFORMANCE__MEMMAP_THRESHOLD_KB=25000
# QDRANT__STORAGE__OPTIMIZERS__DEFAULT_SEGMENT_NUMBER=8

# Hybrid Search & RRF Configuration (Advanced)
# QDRANT__STORAGE__HNSW_INDEX__M=32
# QDRANT__STORAGE__HNSW_INDEX__EF_CONSTRUCT=200
# QDRANT__STORAGE__QUANTIZATION__SCALAR__TYPE=int8
# QDRANT__STORAGE__QUANTIZATION__SCALAR__QUANTILE=0.99
# QDRANT__STORAGE__SEARCH__MAX_RESULTS_PREFETCH=1000
# QDRANT__STORAGE__SEARCH__DIVERSITY_THRESHOLD=0.85

# Neo4j Memory Configuration (Advanced)
# NEO4J_server_memory_heap_initial__size=2G
# NEO4J_server_memory_heap_max__size=4G

# Docker Resource Limits (Advanced)
# These are set in docker-compose.yml but can be overridden
# NEURAL_TOOLS_MEMORY_LIMIT=4G
# NEURAL_STORAGE_MEMORY_LIMIT=4G  
# NEURAL_EMBEDDINGS_MEMORY_LIMIT=8G
# NEO4J_MEMORY_LIMIT=6G

# Setup Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Set a secure NEO4J_PASSWORD (required)
# 3. Customize PROJECT_NAME if running multiple projects
# 4. Uncomment debug ports if you need service access for development
# 5. Run: docker-compose -f config/docker-compose.neural-tools.yml up